# ============================================
# Configuration de l'Application
# ============================================
# Copiez ce fichier vers .env.local et configurez les valeurs selon votre environnement

# ============================================
# Base de données PostgreSQL
# ============================================
# URL de connexion complète à la base de données
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5434/gestion_stages?schema=public"

# Configuration PostgreSQL (pour docker compose)
# Ces variables sont utilisées par docker-compose.yml pour configurer le conteneur PostgreSQL
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=gestion_stages
DB_PORT=5434

# ============================================
# Configuration Groq Cloud (Optionnel)
# ============================================
# Groq Cloud est utilisé pour le parsing intelligent de PDF et le chat assistant
# Si vous n'utilisez pas ces fonctionnalités, vous pouvez ignorer ces variables
# 
# Pour activer Groq:
# 1. Créez un compte sur https://console.groq.com
# 2. Générez une clé API dans la section API Keys
# 3. Configurez GROQ_API_KEY avec votre clé
# 
# Modèles disponibles (par défaut: llama-3.1-8b-instant):
# - llama-3.1-8b-instant (rapide, recommandé)
# - llama-3.1-70b-versatile (plus puissant)
# - mixtral-8x7b-32768 (bon équilibre)
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instant

# ============================================
# Environnement
# ============================================
# Valeurs possibles: development, production, test
# En production, assurez-vous de définir NODE_ENV=production
NODE_ENV=development

# ============================================
# Configuration du Serveur
# ============================================
# Port sur lequel l'application Next.js écoute
# Par défaut: 3000
# Note: Next.js utilise automatiquement PORT si défini, sinon 3000
PORT=3000

# ============================================
# Configuration de l'application
# ============================================
# URL de l'application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# Notes de Configuration
# ============================================
# 
# 1. Base de données:
#    - En développement: Utilisez docker compose up pour lancer PostgreSQL
#    - En production: Configurez une base de données PostgreSQL externe
#    - La variable DATABASE_URL est prioritaire sur DB_USER, DB_PASSWORD, etc.
#
# 2. Groq Cloud:
#    - Optionnel: Nécessaire uniquement pour le parsing de PDF avec IA et le chat
#    - Si non configuré, le parsing utilisera la méthode regex par défaut
#    - Le chat assistant ne sera pas disponible si GROQ_API_KEY n'est pas configurée
#
# 3. Sécurité:
#    - Ne commitez JAMAIS le fichier .env.local dans git
#    - Utilisez des mots de passe forts en production
#    - Changez les valeurs par défaut en production
#
# 4. Docker:
#    - Les variables DB_* sont utilisées par docker-compose.yml
#    - Utilisez la commande "docker compose up" (avec un espace) pour démarrer les services
#    - DATABASE_URL peut être construite à partir de ces variables si nécessaire

